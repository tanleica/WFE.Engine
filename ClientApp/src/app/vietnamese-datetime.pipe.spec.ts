import { VietnameseDatetimePipe } from './vietnamese-datetime.pipe';

describe('VietnameseDatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new VietnameseDatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
